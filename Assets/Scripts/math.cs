using System;
using System.Collections.Generic;


/*public static class math
{
    public static double E = 2.7182818284590451;

    public static List<double> arrSub(double[] a, double[b])
    {
        double[] c;
        for (int i = 0; i < a.Length; i ++)
        {
            c.ad
        }
    }



    public static double sigmoid(double x) {
        // Sigmoid activation function: f(x) = 1 / (1 + e^(-x))
        return 1 / (1 + Math.Pow(E, -x));
    }

    public static double deriv_sigmoid(double x)
    {
        // Derivative of sigmoid: f'(x) = f(x) * (1 - f(x))
        double fx = sigmoid(x);
        return fx * (1 - fx);
    }
    public static double[] mse_loss(double[] y_true, double[] y_pred)
    {
        // y_true and y_pred are numpy arrays of the same length.
        return Math.Pow(y_true - y_pred, 2).mean();
    }
}*/
